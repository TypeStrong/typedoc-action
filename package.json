{
    "scripts": {
        "build": "tsc -p . && ncc build dist/index.js -o dist-bundle --source-map"
    },
    "devDependencies": {
        "@types/node": "^14.14.25",
        "@vercel/ncc": "^0.27.0",
        "typescript": "^4.1.4"
    },
    "dependencies": {
        "@actions/core": "^1.2.6",
        "@actions/exec": "^1.0.4"
    }
}
